import{_ as t}from"./chunks/cynefin-OW5HDTMX.rPCZ-tep.js";import{o as s,c as o,a as n}from"./chunks/mermaid.C1W4YCqa.js";import"./chunks/mermaid-utils.B0bJactl.js";import"./chunks/mermaid-graph.C5JX3QLV.js";import"./chunks/mermaid-layout.DzVnZVT_.js";const k=JSON.parse('{"title":"Level 3: Components","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/components.md","filePath":"architecture/components.md"}'),a={name:"architecture/components.md"};function i(l,e,r,c,d,p){return s(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="level-3-components" tabindex="-1">Level 3: Components <a class="header-anchor" href="#level-3-components" aria-label="Permalink to &quot;Level 3: Components&quot;">​</a></h1><p>The pure-function core is split into two focused diagrams: the <strong>CLI data pipeline</strong> (filter → group → format) and the <strong>TUI render layer</strong> (all display components). Every component is side-effect-free and fully unit-tested.</p><h2 id="_3a-—-cli-data-pipeline" tabindex="-1">3a — CLI data pipeline <a class="header-anchor" href="#_3a-—-cli-data-pipeline" aria-label="Permalink to &quot;3a — CLI data pipeline&quot;">​</a></h2><p>The three pure functions called by the CLI parser to transform raw API results into a filtered, grouped, formatted output.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light-high-contrast github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">%%{init: {&quot;theme&quot;: &quot;base&quot;, &quot;themeVariables&quot;: {&quot;fontFamily&quot;: &quot;Poppins, Aestetico, Arial, sans-serif&quot;, &quot;primaryColor&quot;: &quot;#9933FF&quot;, &quot;primaryTextColor&quot;: &quot;#ffffff&quot;, &quot;lineColor&quot;: &quot;#0000CC&quot;, &quot;tertiaryColor&quot;: &quot;#FFCC33&quot;}, &quot;themeCSS&quot;: &quot;.label,.nodeLabel,.cluster-label &gt; span{font-family:Poppins,Arial,sans-serif;letter-spacing:.2px} .cluster-label &gt; span{font-weight:600;font-size:13px} .edgePath .path{stroke-width:2px}&quot;}}%%</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">C4Component</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  title Level 3a: CLI data pipeline</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateLayoutConfig($c4ShapeInRow=&quot;5&quot;, $c4BoundaryInRow=&quot;1&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Container(cli, &quot;CLI parser&quot;, &quot;github-code-search.ts&quot;, &quot;Orchestrates filter,&lt;br/&gt;group, output and&lt;br/&gt;shell completions&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Container_Boundary(core, &quot;Pure-function core — no I/O&quot;) {</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(regexParser, &quot;Query parser&quot;, &quot;src/regex.ts&quot;, &quot;isRegexQuery()&lt;br/&gt;buildApiQuery()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(aggregate, &quot;Filter &amp; aggregation&quot;, &quot;src/aggregate.ts&quot;, &quot;aggregate()&lt;br/&gt;exclude repos &amp; extracts&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(group, &quot;Team grouping&quot;, &quot;src/group.ts&quot;, &quot;groupByTeamPrefix()&lt;br/&gt;flattenTeamSections()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(outputFn, &quot;Output formatter&quot;, &quot;src/output.ts&quot;, &quot;buildOutput()&lt;br/&gt;markdown or JSON&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(completions, &quot;Shell completions&quot;, &quot;src/completions.ts&quot;, &quot;generateCompletion()&lt;br/&gt;detectShell()&lt;br/&gt;getCompletionFilePath()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(cli, regexParser, &quot;Parse regex&lt;br/&gt;query&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(cli, regexParser, $offsetX=&quot;35&quot;, $offsetY=&quot;-17&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(cli, aggregate, &quot;Filter&lt;br/&gt;CodeMatch[]&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(cli, aggregate, $offsetX=&quot;0&quot;, $offsetY=&quot;-17&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(cli, group, &quot;Group into&lt;br/&gt;TeamSection[]&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(cli, group, $offsetX=&quot;-33&quot;, $offsetY=&quot;-17&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(cli, outputFn, &quot;Format&lt;br/&gt;(non-interactive)&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(cli, outputFn, $offsetX=&quot;-60&quot;, $offsetY=&quot;-17&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(cli, completions, &quot;Generate&lt;br/&gt;script&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(cli, completions, $offsetX=&quot;-90&quot;, $offsetY=&quot;-17&quot;)</span></span></code></pre></div><h2 id="_3b-—-tui-render-layer" tabindex="-1">3b — TUI render layer <a class="header-anchor" href="#_3b-—-tui-render-layer" aria-label="Permalink to &quot;3b — TUI render layer&quot;">​</a></h2><p>The render-layer modules called by the TUI on every redraw. Most live in <code>src/render/</code> and are re-exported through the <code>src/render.ts</code> façade; <code>src/output.ts</code> is the output formatter invoked on confirmation and <code>src/render/filter-match.ts</code> provides shared pattern-matching helpers used by several render modules.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light-high-contrast github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">%%{init: {&quot;theme&quot;: &quot;base&quot;, &quot;themeVariables&quot;: {&quot;fontFamily&quot;: &quot;Poppins, Aestetico, Arial, sans-serif&quot;, &quot;primaryColor&quot;: &quot;#9933FF&quot;, &quot;primaryTextColor&quot;: &quot;#ffffff&quot;, &quot;lineColor&quot;: &quot;#0000CC&quot;, &quot;tertiaryColor&quot;: &quot;#FFCC33&quot;}, &quot;themeCSS&quot;: &quot;.label,.nodeLabel,.cluster-label &gt; span{font-family:Poppins,Arial,sans-serif;letter-spacing:.2px} .cluster-label &gt; span{font-weight:600;font-size:13px} .edgePath .path{stroke-width:2px}&quot;}}%%</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">C4Component</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  title Level 3b: TUI render layer</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateLayoutConfig($c4ShapeInRow=&quot;6&quot;, $c4BoundaryInRow=&quot;1&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Container(tui, &quot;TUI&quot;, &quot;src/tui.ts&quot;, &quot;Calls render functions&lt;br/&gt;on every redraw;&lt;br/&gt;formats output on Enter&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Container_Boundary(render, &quot;src/render/ — pure functions&quot;) {</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(terminal, &quot;Terminal API wrapper&quot;, &quot;src/render/terminal.ts&quot;, &quot;visibleWidth()&lt;br/&gt;stripAnsi()&lt;br/&gt;clipToWidth()&lt;br/&gt;hasAnsi()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(rows, &quot;Row builder&quot;, &quot;src/render/rows.ts&quot;, &quot;buildRows()&lt;br/&gt;rowTerminalLines()&lt;br/&gt;isCursorVisible()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(summary, &quot;Summary builder&quot;, &quot;src/render/summary.ts&quot;, &quot;buildSummary()&lt;br/&gt;buildSummaryFull()&lt;br/&gt;buildSelectionSummary()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(filter, &quot;Filter stats&quot;, &quot;src/render/filter.ts&quot;, &quot;buildFilterStats()&lt;br/&gt;FilterStats — visible/hidden counts&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(selection, &quot;Selection helpers&quot;, &quot;src/render/selection.ts&quot;, &quot;applySelectAll()&lt;br/&gt;applySelectNone()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(highlight, &quot;Syntax highlighter&quot;, &quot;src/render/highlight.ts&quot;, &quot;highlightFragment()&lt;br/&gt;ANSI token colouring&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(teamPick, &quot;Team pick bar&quot;, &quot;src/render/team-pick.ts&quot;, &quot;renderTeamPickHeader()&lt;br/&gt;ANSI candidate bar&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(outputFn, &quot;Output formatter&quot;, &quot;src/output.ts&quot;, &quot;buildOutput()&lt;br/&gt;markdown or JSON&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">    Component(filterMatch, &quot;Pattern matchers&quot;, &quot;src/render/filter-match.ts&quot;, &quot;makeExtractMatcher()&lt;br/&gt;makeRepoMatcher()&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(tui, rows, &quot;Build terminal&lt;br/&gt;rows&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(tui, rows, $offsetX=&quot;-1&quot;, $offsetY=&quot;-15&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(tui, summary, &quot;Build header &lt;br&gt;/ footer&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(tui, summary, $offsetX=&quot;-44&quot;, $offsetY=&quot;-16&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(tui, filter, &quot;Build&lt;br/&gt;status bar&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(tui, filter, $offsetX=&quot;-64&quot;, $offsetY=&quot;-16&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(tui, selection, &quot;Select all&lt;br/&gt;/ none&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(tui, selection, $offsetX=&quot;-105&quot;, $offsetY=&quot;-12&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(tui, highlight, &quot;Highlight&lt;br/&gt;extracts&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(tui, highlight, $offsetX=&quot;-150&quot;, $offsetY=&quot;-16&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(tui, teamPick, &quot;Render pick&lt;br/&gt;mode bar&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(tui, teamPick, $offsetX=&quot;-180&quot;, $offsetY=&quot;-16&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(tui, outputFn, &quot;Format&lt;br/&gt;on Enter&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(tui, outputFn, $offsetX=&quot;17&quot;, $offsetY=&quot;160&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(rows, filterMatch, &quot;Uses pattern&lt;br/&gt;matchers&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(rows, filterMatch, $offsetX=&quot;-5&quot;, $offsetY=&quot;-5&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(filter, filterMatch, &quot;Uses pattern&lt;br/&gt;matchers&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(filter, filterMatch, $offsetX=&quot;45&quot;, $offsetY=&quot;-5&quot;)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  Rel(selection, filterMatch, &quot;Uses pattern&lt;br/&gt;matchers&quot;)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#E1E4E8;">  UpdateRelStyle(selection, filterMatch, $offsetX=&quot;165&quot;, $offsetY=&quot;-25&quot;)</span></span></code></pre></div><h2 id="component-descriptions" tabindex="-1">Component descriptions <a class="header-anchor" href="#component-descriptions" aria-label="Permalink to &quot;Component descriptions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Component</th><th>Source file</th><th>Key exports</th></tr></thead><tbody><tr><td><strong>Filter &amp; aggregation</strong></td><td><code>src/aggregate.ts</code></td><td><code>aggregate()</code> — filters <code>CodeMatch[]</code> by repository and extract exclusion lists; normalises both <code>repoName</code> and <code>org/repoName</code> forms.</td></tr><tr><td><strong>Team grouping</strong></td><td><code>src/group.ts</code></td><td><code>groupByTeamPrefix()</code> — groups <code>RepoGroup[]</code> into <code>TeamSection[]</code> keyed by team slug; <code>flattenTeamSections()</code> — converts back to a flat list for the TUI row builder; <code>applyTeamPick()</code> — moves repos from a combined section to a chosen team section; <code>rebuildTeamSections()</code> — reconstructs <code>TeamSection[]</code> from a flat list (used by TUI pick mode).</td></tr><tr><td><strong>Shell completions</strong></td><td><code>src/completions.ts</code></td><td><code>generateCompletion(shell)</code> — returns the full bash/zsh/fish completion script; <code>detectShell()</code> — reads <code>$SHELL</code>; <code>getCompletionFilePath(shell, opts)</code> — resolves the XDG-aware installation path.</td></tr><tr><td><strong>Layout constants</strong></td><td><code>src/render/layout-constants.ts</code></td><td><code>getHeaderLines()</code> — computes visible header height based on filter mode and active filter state; Mouse button constants: <code>MOUSE_BUTTON_WHEEL_UP</code> (64), <code>MOUSE_BUTTON_WHEEL_DOWN</code> (65); Column zones: <code>FOLD_COLUMN_START/END</code>, <code>CHECKBOX_COLUMN_START/END</code>, <code>NAV_COLUMN_START</code>; Zone helpers: <code>isClickInFoldZone()</code>, <code>isClickInCheckboxZone()</code>, <code>isClickInNavZone()</code>.</td></tr><tr><td><strong>Row builder</strong></td><td><code>src/render/rows.ts</code></td><td><code>buildRows()</code> — converts <code>RepoGroup[]</code> into <code>Row[]</code> filtered by the active target (path / content / repo); <code>rowTerminalLines()</code> — measures wrapped height; <code>isCursorVisible()</code> — viewport clipping.</td></tr><tr><td><strong>Mouse hit-test</strong></td><td><code>src/render/mouse-hit.ts</code></td><td><code>hitTestClick()</code> — maps terminal coordinates (x, y) to logical row and action (fold, select, navigate); handles full-width checkbox zone. (Double-click detection is implemented separately in <code>src/tui.ts</code> via timestamp tracking.)</td></tr><tr><td><strong>Summary builder</strong></td><td><code>src/render/summary.ts</code></td><td><code>buildSummary()</code> — compact header line; <code>buildSummaryFull()</code> — detailed counts; <code>buildSelectionSummary()</code> — &quot;N files selected&quot; footer.</td></tr><tr><td><strong>Filter stats</strong></td><td><code>src/render/filter.ts</code></td><td><code>buildFilterStats()</code> — produces the <code>FilterStats</code> object (visible repos, files, matches) used by the TUI filter bar live counter.</td></tr><tr><td><strong>Pattern matchers</strong></td><td><code>src/render/filter-match.ts</code></td><td><code>makeExtractMatcher()</code> — builds a case-insensitive substring or RegExp test function for path or content targets; <code>makeRepoMatcher()</code> — wraps the same logic for repo-name matching.</td></tr><tr><td><strong>Selection helpers</strong></td><td><code>src/render/selection.ts</code></td><td><code>applySelectAll()</code> — marks all visible rows as selected (respects filter target); <code>applySelectNone()</code> — deselects all visible rows.</td></tr><tr><td><strong>Syntax highlighter</strong></td><td><code>src/render/highlight.ts</code></td><td><code>highlightFragment()</code> — maps file extension to a language token ruleset and applies ANSI escape sequences. Falls back to plain text for unknown extensions.</td></tr><tr><td><strong>Team pick bar</strong></td><td><code>src/render/team-pick.ts</code></td><td><code>renderTeamPickHeader()</code> — renders the ANSI pick-mode candidate bar shown when the user presses <code>p</code> on a multi-team section header. Focused candidate is highlighted in bold magenta; others are dimmed.</td></tr><tr><td><strong>Terminal API wrapper</strong></td><td><code>src/render/terminal.ts</code></td><td><code>visibleWidth()</code> — measures terminal columns (Bun.stringWidth); <code>stripAnsi()</code> — removes escape codes (Bun.stripANSI); <code>clipToWidth()</code> — truncates to N columns preserving partial reset (Bun.sliceAnsi); <code>hasAnsi()</code> — detects presence of codes. Sole authorized call site for Bun 1.4+ ANSI APIs.</td></tr><tr><td><strong>Output formatter</strong></td><td><code>src/output.ts</code></td><td><code>buildOutput()</code> — entry point for both <code>--format markdown</code> and <code>--format json</code> serialisation of the confirmed selection.</td></tr></tbody></table><h2 id="design-principles" tabindex="-1">Design principles <a class="header-anchor" href="#design-principles" aria-label="Permalink to &quot;Design principles&quot;">​</a></h2><ul><li><strong>No I/O.</strong> Every component in this layer is a pure function: given the same inputs it always returns the same outputs. This makes them straightforward to test with Bun&#39;s built-in test runner.</li><li><strong>Single responsibility.</strong> Each component owns exactly one concern (rows, summary, selection, …). The TUI composes them at render time rather than duplicating logic.</li><li><strong><code>types.ts</code> as the contract.</strong> All components share the interfaces defined in <code>src/types.ts</code> (<code>TextMatchSegment</code>, <code>TextMatch</code>, <code>CodeMatch</code>, <code>RepoGroup</code>, <code>Row</code>, <code>TeamSection</code>, <code>OutputFormat</code>, <code>OutputType</code>, <code>FilterTarget</code>). Changes to these types require updating all components.</li><li><strong><code>render.ts</code> as façade.</strong> External consumers import from <code>src/render.ts</code>, which re-exports all symbols from the <code>src/render/</code> sub-modules plus the top-level <code>renderGroups()</code> and <code>renderHelpOverlay()</code> functions. <code>renderTeamPickHeader</code> is consumed internally by <code>render.ts</code> and is not re-exported (it is not part of the public façade).</li></ul><h2 id="mouse-interaction-model" tabindex="-1">Mouse interaction model <a class="header-anchor" href="#mouse-interaction-model" aria-label="Permalink to &quot;Mouse interaction model&quot;">​</a></h2><p>The TUI supports mouse input via the terminal&#39;s SGR (Select-Graphic-Rendition) protocol, which sends click and scroll events as terminal escape sequences. Mouse and keyboard shortcuts are fully complementary: every mouse action has a keyboard equivalent.</p><h3 id="protocol-and-coordinate-mapping" tabindex="-1">Protocol and coordinate mapping <a class="header-anchor" href="#protocol-and-coordinate-mapping" aria-label="Permalink to &quot;Protocol and coordinate mapping&quot;">​</a></h3><ul><li><strong>Terminal protocol</strong>: SGR 1006 extends basic mouse reporting (<code>?1000</code>) with extended button codes for wheel events and 8-bit-clean 1-indexed coordinates.</li><li><strong>Button codes</strong>: 0 = left click, 1 = middle, 2 = right, 64 = wheel up, 65 = wheel down.</li><li><strong>Coordinate origin</strong>: Terminal coordinates are 1-indexed from the top-left; the TUI converts them to 0-indexed logical row indices via <code>clickedLineOffset = y - 1 - headerLines</code>.</li><li><strong>Header height</strong>: The header (title, summary, filter bar) consumes 4–6 terminal rows depending on filter mode. <code>getHeaderLines()</code> computes this dynamically.</li></ul><h3 id="click-zones-and-hit-testing" tabindex="-1">Click zones and hit-testing <a class="header-anchor" href="#click-zones-and-hit-testing" aria-label="Permalink to &quot;Click zones and hit-testing&quot;">​</a></h3><p>The TUI divides each row into three non-overlapping zones for different interactions:</p><p><strong>Repo rows</strong> (<code>▸ ✓ repo-name</code>):</p><ol><li><strong>Fold zone</strong> (columns 1–2): Double-click toggles the fold state (show/hide extracts).</li><li><strong>Navigation zone</strong> (column 3): Single-click only (move cursor). No double-click action.</li><li><strong>Checkbox zone</strong> (columns 4+, full-width): Double-click toggles repo selection (cascades to all extracts).</li></ol><p><strong>Extract rows</strong> (<code> ✓ path:line:col</code>):</p><ol><li><strong>Navigation zone</strong> (columns 1–3): Single-click only (move cursor).</li><li><strong>Checkbox zone</strong> (columns 4+, full-width): Double-click toggles extract selection.</li></ol><p>Each zone occupies visual terminal columns; emoji characters (▸, ✓) are 2 columns wide. Zone boundaries are defined in <code>layout-constants.ts</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-high-contrast github-dark vp-code" tabindex="0"><code><span class="line"><span>Repo row visual layout:</span></span>
<span class="line"><span>Column:  1   2   3   4   5   6   7  ...</span></span>
<span class="line"><span>Content: ▸       │   ✓       r e p o - n a m e</span></span>
<span class="line"><span>Zone:    └fold─┘ nav └─────checkbox─────┘</span></span></code></pre></div><p>The full-width checkbox zone (from column 4 to screen edge) enables natural double-click selection anywhere on the row content, not just on the checkbox emoji.</p><h3 id="double-click-detection" tabindex="-1">Double-click detection <a class="header-anchor" href="#double-click-detection" aria-label="Permalink to &quot;Double-click detection&quot;">​</a></h3><p>Double-clicks are detected by comparing:</p><ol><li>The focused row (via <code>getRowKey()</code> which combines row type and indices).</li><li>The time delta: less than 300 ms since the previous click on the same row.</li></ol><p>If both conditions are met, the double-click action is executed (fold or select); otherwise, the click is treated as a single navigation action.</p><h3 id="scroll-and-cooldown" tabindex="-1">Scroll and cooldown <a class="header-anchor" href="#scroll-and-cooldown" aria-label="Permalink to &quot;Scroll and cooldown&quot;">​</a></h3><p>Wheel scroll events are processed by a state machine in <code>scroll-cooldown.ts</code>:</p><ol><li><strong>Scroll request</strong>: Wheel up/down updates <code>scrollOffset</code> by <code>MOUSE_SCROLL_STEP</code> (3 rows).</li><li><strong>Cooldown window</strong>: A 600 ms timer starts; all clicks are ignored during this period.</li><li><strong>Momentum scrolling</strong>: On trackpads (macOS, Linux), scroll gestures decelerate after the wheel event. The cooldown prevents accidental clicks during this deceleration phase.</li></ol><h3 id="mouse-event-parsing" tabindex="-1">Mouse event parsing <a class="header-anchor" href="#mouse-event-parsing" aria-label="Permalink to &quot;Mouse event parsing&quot;">​</a></h3><p>Mouse escape sequences are parsed in <code>src/render/mouse.ts</code> via <code>parseMouseEvent()</code>, which extracts button code and coordinates. Non-mouse input is passed to the normal keyboard handler.</p><h3 id="integration-with-tui-state" tabindex="-1">Integration with TUI state <a class="header-anchor" href="#integration-with-tui-state" aria-label="Permalink to &quot;Integration with TUI state&quot;">​</a></h3><p>The TUI state machine (<code>src/tui.ts</code>) integrates mouse events into the redraw loop:</p><ol><li>Parse the mouse event (or keyboard input).</li><li>If mouse: check scroll cooldown and hit-test the click against visible rows.</li><li>Compute the appropriate action and update state (cursor position, selection state, fold state).</li><li>Redraw the screen.</li></ol><p>All rendering state (row visibility, cursor position, selection) is independent of mouse vs. keyboard input: the view is always consistent regardless of how the user interacts with the TUI.</p>`,38)])])}const E=t(a,[["render",i]]);export{k as __pageData,E as default};
